<%= turbo_stream.replace "new_room_form" do %>
  <%= form_with(model: Room.new, id: "new_room_form") do |form| %>
    <%= form.text_field :name, placeholder: "Room name..." %>
    <%= form.submit "Create" %>
  <% end %>
<% end %>
